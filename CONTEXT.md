# Entry d20 领域词汇

这个上下文描述个人网站的 d20 入口体验，以及骰子结果未来可能触发的站点体验。

## 结果与效果

**骰子结果**：用户一次掷骰产生的体验触发，不等同于某篇文章。结果可以触发文章推荐、站点导航、页面主题或其他交互效果。

**结果池**：当前启用、可被骰子抽取的骰子结果集合。结果池与 d20 点数解耦，默认等概率，并允许通过权重调节出现概率。

**临时效果**：只在当前页面交互中生效的结果，例如推荐文章、提示语或短动画；通常不需要写入 URL，也不跨刷新持久化。

**持久效果**：会改变站点状态并在一段时间内保留的结果，例如页面主题。效果从最近一次触发开始持续滚动 24 小时；新的持久结果会覆盖旧效果并重新计时，超过 24 小时后恢复默认状态。

**默认入口状态**：没有有效持久效果时，站点入口应呈现的基础 d20 体验与页面外观。

## 入口交互

**游戏式引导**：入口界面通过可交互物体的视觉状态和即时反馈自然提示操作，而不依赖说明文字。第一版中，d20 的呼吸光表示当前可以投掷；用户开始拖拽或投掷时光效收起，骰子停稳后恢复。
